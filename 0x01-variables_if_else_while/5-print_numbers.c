#include <stdio.h>
/**
 * main - Program that prints all single digit numbers of base ten
 *
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
